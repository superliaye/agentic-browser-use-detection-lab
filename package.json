{
  "name": "agentic-browser-use-detection-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test:compile": "tsc --project tsconfig.test.json",
    "test": "npm run test:compile && node --test tests/*.test.mjs",
    "build": "npm run typecheck && vite build"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vite": "8.2.1"
  }
}
